//- 믹스인으로 활용할 객체 이름(믹스인이 받을 객체명)
mixin video(video)
    div 
        h4
            a(href=`/videos/${video.id}`)=video.title
        
        p=video.description
        small=video.createdAt
        hr
