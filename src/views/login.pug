extends base

block content 
    if errorMessage 
        span=errorMessage
    form(method="POST")
        input(name="username", type="text",required, placeholder="아이디")
        input(name="password", type="password",required, placeholder="비밀번호")
        input(type="submit", value="로그인")
        br
        a(href="/users/github/start") GitHub 로 계속하기 &rarr;
    hr
    div
        span 계정이 없으신가요?  
        a(href="/join") 가입하기 &rarr;