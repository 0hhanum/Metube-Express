extends ../base
include ../mixins/video

block content 
    div.profile__data
        if user.avatarUrl && user.socialOnly === false
            img(src=user.avatarUrl, width="100").avatar
        else if user.avatarUrl && user.socialOnly === true
            img(src=user.avatarUrl, width="100").avatar
        else 
            div.avatar-box
                span #{user.name.charAt(0)}
        h3=user.name
    div.video-grid
        each video in user.videos
            +video(video)
        else 
            span.empty__message 영상이 존재하지 않습니다 